apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: mtrack
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}